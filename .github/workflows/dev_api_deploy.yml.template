name: dev_api_deploy.yml
on:
  push:
    branches:
      - dev

jobs:

#  test:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up Python
#        uses: actions/setup-python@v4
#        with:
#          python-version: '3.13.9'
#
#      - name: Install dependencies
#        run: |
#          pip install -r requirements.txt
#          pip install pytest pytest-cov
#
#      - name: Run tests
#        run: pytest --cov=app tests/

  build:
    runs-on: self-hosted
#    needs: test
    if: github.ref == 'refs/heads/dev'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run migrations from HOST (not container)
        run: |
          cd /PATH/TO/YOUR/PROJECT
          export DATABASE_URL=postgresql://dev_user:jg111kas63ff@database_dev:5432/sessionbloom_dev
          alembic upgrade head

      - name: Rebuild and restart containers
        run: |
          cd /PATH/TO/YOUR/PROJECT
          docker-compose -f dev_db.docker-compose.yml down
          docker-compose -f dev_db.docker-compose.yml up -d --build
          docker image prune -f
